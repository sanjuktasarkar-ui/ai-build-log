[project]
name = "ai-build-log"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sanjukta Sarkar", email = "sanjukta.sarkar@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.5.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
ai-build-log = "ai_build_log:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
